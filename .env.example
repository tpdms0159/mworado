# Supabase — 프로젝트 설정 > API 에서 확인 (README 참고)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 서버에서만 사용. 절대 NEXT_PUBLIC_ 접두사를 붙이지 말 것 (클라이언트 노출 금지)
SUPABASE_SERVICE_ROLE_KEY=

# Vercel Cron(/api/cron/keepalive)이 호출 시 검증용 비밀값 — 아무 문자열이나 생성해서 사용
CRON_SECRET=
