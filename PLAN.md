# 뭐라도해야지 — 기획 프롬프트

> **사용법**: 아래 `─── 프롬프트 시작 ───` 부터 `─── 프롬프트 끝 ───` 까지를 통째로 복사해서 Claude Code(또는 Cursor)에 붙여넣으세요.
> 처음엔 **Plan 모드**로 실행해 계획을 먼저 확인한 뒤 구현에 들어가는 걸 권합니다.

---

─── 프롬프트 시작 ───

## 역할

너는 이 프로젝트의 **테크리드 겸 프로덕트 디자이너**야. 내가 준 요구사항을 바탕으로, 실제로 배포되어 남들에게 보여줄 수 있는 수준의 웹앱을 처음부터 끝까지 설계하고 구현해줘.

## 진행 방식 (중요)

1. **바로 코드부터 짜지 마.** 먼저 아래 요구사항을 읽고 ① 확인이 필요한 애매한 지점 3~5개를 질문하고, ② 전체 구현 계획(파일 구조, 데이터 모델, 마일스톤)을 제시해. 내가 승인하면 그때 구현 시작.
2. 구현은 **마일스톤 단위**로 진행하고, 각 마일스톤이 끝날 때마다 로컬에서 실행 가능한 상태로 만들고 나에게 확인을 받아.
3. 결정을 내릴 때마다 "왜 이 선택인지" 한 줄로 근거를 남겨줘. 특히 무료 티어 제약과 관련된 선택은 반드시.
4. 나는 개발 전문가가 아니라 **아임웹 기반으로 홈페이지를 제작하는 사람**이야. 터미널 명령어나 외부 서비스(Supabase, Vercel) 설정이 필요하면 클릭 순서까지 구체적으로 알려줘.

---

## 1. 제품 개요

**서비스 이름**: 뭐라도해야지

**한 줄 정의**: 매일 해야 할 일을 등록하고 완료를 체크하며, 반복 루틴의 꾸준함을 잔디 그래프로 확인하는 개인용 데일리 체크 웹앱.

**이름이 담은 태도 (제품 전반의 톤을 여기에 맞출 것)**
- "완벽하게 다 해내자"가 아니라 **"오늘 하나라도 하자"**. 이름 자체가 부담을 낮추는 방향이야.
- 그래서 미완료 항목을 빨간색·경고·"실패"로 표현하지 않는다. 채근하지 않고, 한 칸이라도 채워진 걸 인정해주는 화법.
- 카피 톤은 구어체 반말에 가까운 담백한 말투. 예: 빈 상태 문구 "오늘은 아직 아무것도 없네. 뭐라도 하나 적어볼까?", 전부 완료했을 때 "오늘 할 일 끝. 잘했어."
- 과한 게임화(뱃지·레벨·포인트)는 넣지 않는다. 잔디와 연속일까지만.

**해결하려는 문제**
- 기존 투두 앱은 기능이 너무 많아 "오늘 뭘 할지"에 집중하기 어렵다.
- 일회성 할 일과 매일 반복하는 습관이 한 목록에 섞여 관리가 안 된다.
- 며칠 하다 그만두는 이유는 **꾸준함이 눈에 보이지 않기 때문**이다.

**핵심 가치 (이 3개를 벗어나는 기능은 후순위로 미룰 것)**
1. 오늘 화면 하나로 끝난다 — 열자마자 오늘 할 일만 보인다.
2. 반복 루틴은 자동으로 생성된다 — 매일 다시 적을 필요 없다.
3. 해온 기록이 시각적으로 쌓인다 — 잔디와 연속일이 동기가 된다.

**이 프로젝트의 두 번째 목적**: 이건 내 **웹 제작 포트폴리오**로도 쓸 서비스야. 그래서 로그인 없이 둘러볼 수 있는 랜딩 페이지와 데모 체험이 반드시 필요하고, UI 완성도가 기능 개수보다 중요해.

## 2. 대상 사용자

- 주 사용자: 20~30대 직장인·프리랜서. 노션이나 종이 다이어리를 쓰다 지친 사람.
- 사용 맥락: 아침에 폰으로 열어 오늘 할 일 확인 → 하루 중 틈틈이 체크 → 밤에 잔디 확인.
- **모바일이 1순위**. 데스크탑은 넓은 화면에 맞춰 확장되면 충분.

## 3. 기술 스택 (전부 무료 티어)

| 영역 | 선택 | 비고 |
|---|---|---|
| 프레임워크 | Next.js (App Router) + TypeScript | |
| 스타일 | Tailwind CSS + shadcn/ui | |
| DB / 인증 | Supabase (Postgres + Auth) | 무료 티어 |
| 배포 | Vercel | 무료 티어, GitHub 연동 자동 배포 |
| 상태관리 | 서버 컴포넌트 우선 + 클라이언트는 최소한 | 과한 라이브러리 금지 |
| 차트 | 잔디는 직접 구현(div grid), 그 외 필요시 Recharts | |

**스택 관련 제약**
- 유료 전환이 필요한 서비스는 절대 도입하지 마. 대안이 없으면 나에게 먼저 물어봐.
- 로그인은 **이메일 + 비밀번호** 방식. 가입 시에만 메일로 링크를 보내 비밀번호를 설정하고, 이후에는 아이디(이메일)/비밀번호로 로그인한다. 구글 OAuth는 후속 과제.
  - _(2026-09-07 변경: 원래 "매직링크 + 구글, 비밀번호 방식은 안 씀"이었으나, 매번 메일을 받는 번거로움과 발송 한도 문제로 이메일+비밀번호 방식으로 전환. 가입 링크 발송에는 매직링크 메커니즘을 그대로 재사용한다.)_
- Supabase 무료 프로젝트는 일정 기간 활동이 없으면 일시정지될 수 있어. 이걸 방지할 방법(주기적 핑 등)과 한계를 README에 명시해줘.
- 환경변수는 `.env.local` + `.env.example`로 분리하고, 서비스 롤 키가 클라이언트에 노출되지 않게 해.

## 4. 데이터 모델 (설계 시 반드시 지킬 원칙)

테이블 스키마는 네가 설계하되, 아래 원칙은 반드시 반영해:

1. **반복 루틴은 "템플릿"과 "그날의 기록"을 분리한다.**
   - 루틴 템플릿(예: "물 2L 마시기", 매일 / 월수금 / 주 3회)과, 날짜별 완료 기록을 별도로 저장.
   - 사용자가 루틴 이름을 바꾸거나 삭제해도 **과거 기록은 훼손되지 않아야** 한다.
2. **오늘 화면은 "일회성 할 일"과 "오늘 해당하는 루틴"을 합쳐서 보여준다.** 저장 구조는 달라도 사용자에겐 하나의 목록.
3. **날짜는 사용자 타임존(기본 Asia/Seoul) 기준의 날짜(date)로 저장한다.** UTC 타임스탬프만 저장해서 자정 근처에 날짜가 밀리는 버그가 절대 없어야 해.
4. **"하루의 시작 시각" 설정을 지원한다** (기본 새벽 4시). 밤 12시~4시에 체크한 것은 전날로 집계.
5. **모든 테이블에 RLS(Row Level Security)를 건다.** 다른 사용자의 데이터가 절대 조회되지 않도록 정책을 작성하고, 정책 SQL도 마이그레이션 파일로 남겨줘.
6. 삭제는 물리 삭제 대신 soft delete를 고려해, 통계가 깨지지 않게.

## 5. 화면 구조

| 경로 | 화면 | 설명 |
|---|---|---|
| `/` | 랜딩 | 비로그인. 서비스 소개, 스크린샷, "체험해보기" / "시작하기" |
| `/demo` | 데모 | 로그인 없이 로컬 상태로 전부 체험. 새로고침하면 초기화, 상단에 안내 배너 |
| `/login` | 로그인 | 이메일 + 비밀번호 (가입은 `/signup`, 재설정은 `/forgot-password`) |
| `/today` | **오늘** (메인) | 오늘의 할 일 + 루틴 목록, 체크, 진행률 |
| `/routines` | 루틴 관리 | 반복 할 일 생성·수정·보관 |
| `/stats` | 기록 | 잔디 그래프, 연속일, 루틴별 달성률 |
| `/settings` | 설정 | 하루 시작 시각, 타임존, 테마, 계정, 데이터 내보내기·계정 삭제 |

## 6. 기능 명세

### P0 — 이게 없으면 서비스가 아니다

**할 일 기본**
- 오늘 화면에서 한 줄 입력으로 즉시 추가 (엔터로 연속 추가, 입력창이 항상 접근 가능한 위치에)
- 체크 / 체크 해제 — **낙관적 업데이트**로 즉시 반응, 실패 시 롤백 + 토스트
- 인라인 수정, 삭제 (삭제는 "실행 취소" 토스트 5초 제공)
- 순서 변경 (드래그 앤 드롭, 모바일 터치 지원)
- 완료 항목은 목록 하단으로 이동하거나 접기 — 어느 쪽이 좋을지 근거와 함께 제안해줘
- 오늘 진행률 표시 (예: 5/8, 진행 바)

**반복 할 일 / 습관**
- 반복 주기: 매일 / 특정 요일 / 주 N회 / 격일
- 오늘 해당하는 루틴이 매일 자동으로 오늘 목록에 나타남 (별도 생성 버튼 없이)
- 루틴별 현재 연속일(스트릭) 표시
- 루틴 보관(archive) — 삭제하지 않고 목록에서만 내림
- **스트릭 계산 규칙을 코드 주석과 README에 명시**: "주 3회" 루틴에서 연속의 정의, 하루 빠졌을 때 처리, 미래 날짜 처리

**기록 / 통계**
- **잔디 그래프**: 최근 1년, 일자별 완료율에 따라 5단계 농도, 칸에 마우스 올리면(모바일은 탭) 해당 날짜 요약
- 현재 연속일 / 최장 연속일
- 최근 30일 완료율
- 루틴별 달성률 막대
- 데이터가 없는 신규 사용자용 **빈 상태(empty state) 화면**을 반드시 디자인해줘 — 잔디가 텅 빈 화면이 첫인상이 되면 안 됨

**계정**
- 이메일 + 비밀번호 로그인, 가입/비밀번호 재설정, 비밀번호 변경, 로그아웃
- 데이터 JSON 내보내기, 계정 삭제

### P1 — 있으면 좋다 (P0 완료 후 진행)
- 날짜 이동 (어제/특정 날짜 기록 보기 및 수정)
- 다크 모드
- PWA (홈 화면에 추가, 오프라인에서 조회 가능)
- 할 일 메모 / 간단한 카테고리 태그

### P2 — 이번엔 만들지 않는다 (문서에만 남김)
- 알림·푸시, 협업·공유, 캘린더 연동, 하위 할 일, 첨부파일, 결제

> **범위 관리**: P2에 있는 걸 내가 중간에 요청하더라도, "이건 P2인데 지금 넣으면 P0 일정이 밀린다"고 한 번은 짚어줘.

## 7. 디자인 방향

- 톤: 차분하고 여백이 넉넉한, 도구답게 조용한 UI. 알록달록하지 않게.
- 포인트 컬러 1개 + 무채색 위주. 잔디 그래프의 농도 단계는 이 포인트 컬러의 명도 변주로.
- 타이포: 한글 가독성 우선(Pretendard 등), 본문 16px 이상.
- 체크 동작에 짧고 만족스러운 마이크로 인터랙션 (과하지 않게, 200ms 이내).
- 모바일에서 **한 손 조작** 고려 — 주요 액션은 화면 하단부에.
- 접근성: 키보드만으로 전체 조작 가능, 색상 대비 WCAG AA, 체크박스에 적절한 aria 속성.
- 랜딩 페이지는 실제 서비스 화면 스크린샷 중심으로. 과장된 마케팅 문구 대신 화면이 설명하게.

**브랜딩 (이름: 뭐라도해야지)**
- 로고는 한글 워드마크로. 별도 그래픽 심볼 없이 타이포만으로 가고, 파비콘은 첫 글자 "뭐" 또는 체크 형태 중 어울리는 쪽을 제안해줘.
- 이름이 길어서 좁은 화면에서 줄바꿈이 어색해질 수 있어. 모바일 헤더에서의 처리 방식(축약 여부 포함)을 제안해줘.
- 영문 표기가 필요한 곳(도메인, 저장소명, 패키지명)은 `mworado`로 통일.
- 메타 정보에 이름을 반영: `<title>`, OG 태그, PWA manifest의 `name`은 "뭐라도해야지", `short_name`은 "뭐라도".
- 랜딩 히어로 카피는 이름의 태도를 그대로 살릴 것. 방향 예시: "완벽하지 않아도 돼. 오늘 뭐라도 하나만." — 이 톤으로 3안을 제안해줘.

## 8. 비기능 요구사항

- 모바일 Lighthouse 성능·접근성 90점 이상 목표
- 첫 로딩 시 오늘 화면이 1초 안에 의미 있는 콘텐츠를 보여줄 것
- 오류 처리: 네트워크 실패 시 사용자가 이해할 수 있는 한글 메시지, 콘솔 에러만 남기고 끝내지 말 것
- SEO: 랜딩 페이지 메타 태그, OG 이미지, sitemap
- 코드: 컴포넌트 단위 분리, 매직넘버 금지, 날짜 계산 로직은 별도 유틸로 모으고 **단위 테스트 작성**

## 9. 마일스톤

- **M0 — 셋업**: 프로젝트 생성, Supabase 연결, 인증 동작, Vercel 첫 배포까지. *(이 단계에서 배포 URL이 나와야 함)*
- **M1 — 오늘 화면**: 할 일 CRUD, 체크, 정렬, 진행률
- **M2 — 루틴**: 루틴 CRUD, 자동 생성, 스트릭
- **M3 — 기록**: 잔디, 통계, 빈 상태
- **M4 — 마감**: 랜딩, 데모 모드, 설정, 다크모드, 반응형 점검, Lighthouse 개선
- **M5 — 검수**: 체크리스트 기반 QA, README 작성, 배포

## 10. 산출물

- 실행 가능한 전체 소스 코드
- `README.md` — 로컬 실행 방법, 환경변수 설명, Supabase 테이블·RLS 설정 순서, 배포 순서, 무료 티어 제약과 한계
- `docs/spec.md` — 확정된 데이터 모델과 기능 명세 (변경 시 함께 갱신)
- Supabase 마이그레이션 SQL 파일
- 배포된 URL

## 11. 마지막 검수 (M5에서 이걸로 자체 점검하고 결과를 보고해줘)

- [ ] 새 계정으로 가입 → 할 일 추가 → 체크 → 로그아웃 → 재로그인 시 데이터 유지
- [ ] 다른 계정으로 로그인했을 때 남의 데이터가 절대 안 보임 (RLS 실제 검증)
- [ ] 밤 23:50 / 00:10에 체크했을 때 날짜가 의도대로 집계됨
- [ ] 루틴을 수정·삭제해도 과거 잔디 기록이 그대로 남음
- [ ] 데이터가 하나도 없는 신규 계정에서 모든 화면이 깨지지 않음
- [ ] 모바일(375px)·태블릿·데스크탑에서 레이아웃 정상
- [ ] 네트워크를 끊은 상태에서 체크 시 적절한 오류 안내
- [ ] 키보드만으로 할 일 추가·체크·삭제 가능
- [ ] 데모 모드가 로그인 없이 완전히 동작함

─── 프롬프트 끝 ───

---

## 붙여넣기 전에 한 번 손볼 만한 곳

- **기술 스택**: Next.js + Supabase는 무료 배포 조합으로 무난하지만, 익숙한 스택이 따로 있으면 3번 표만 바꾸면 됩니다.
- **P1 항목**: 다크모드나 PWA를 처음부터 넣고 싶으면 P0으로 올리세요.
- **디자인 톤**: 7번을 위브웹 작업 톤에 맞게 고쳐두면 포트폴리오 일관성이 생깁니다.
